let pattern = [];
let playerPattern = [];
let score = 0;


function runGame() {
    
}

function nextLevel () {
    ++score;
}

function pathBuild() {
    if (score = 1) {
        const tiles = ['1', '2', '3'];
        const random = tiles[Math.floor(Math.random() * 3 )];
        return random;
    } else if (score = 2) {
        
    }
}